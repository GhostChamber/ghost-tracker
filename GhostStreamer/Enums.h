#pragma once

enum class StreamType
{
	REPLICATED = 0,
	INDIVIDUAL = 1,

	NUM_STREAM_TYPES = 2
};


enum class CommandState
{
	LISTENING,
	WAITING_FIRST_POINT,
	WAITING_SECOND_POINT
};

enum class HotkeyID
{
	COMMAND_VIEWPORT_1,
	COMMAND_VIEWPORT_2,
	COMMAND_VIEWPORT_3,
	COMMAND_VIEWPORT_4,
	TOGGLE_STREAM_TYPE,
	COMMAND_REPLICATED_VIEWPORT
};